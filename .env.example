# Retainr Configuration Example
# Copy this file to .env and customize as needed

# ======================================================
# DEPLOYMENT MODE
# ======================================================

# Choose deployment mode: native, docker, or auto
# - native: Run MCP server natively with Python virtual environment (RECOMMENDED)
# - docker: Run MCP server in Docker container (legacy mode)
# - auto: Automatically detect best mode based on available setup
RETAINR_MODE=native

# ======================================================
# STORAGE CONFIGURATION
# ======================================================

# Directory for storing memory files (relative to project root)
RETAINR_MEMORY_DIR=./memory

# Example: Use a specific folder for memories
# RETAINR_MEMORY_DIR=/Users/username/Documents/retainr-memories

# Model cache directory (for sentence-transformers models)
RETAINR_MODEL_CACHE_DIR=~/.cache/retainr

# ======================================================
# CHROMADB CONFIGURATION
# ======================================================

# ChromaDB server settings (Docker service)
RETAINR_CHROMA_HOST=localhost
RETAINR_CHROMA_PORT=8000
RETAINR_CHROMA_COLLECTION=retainr_memories

# ======================================================
# MCP SERVER CONFIGURATION
# ======================================================

# MCP transport type (stdio for Claude Code integration)
RETAINR_MCP_TRANSPORT=stdio

# Embedding model for semantic search
RETAINR_EMBEDDING_MODEL=all-MiniLM-L6-v2

# ======================================================
# DEVELOPMENT CONFIGURATION
# ======================================================

# Development mode (enables auto-reload)
RETAINR_DEBUG=false

# ======================================================
# TESTING CONFIGURATION
# ======================================================

# Test mode for pytest
RETAINR_TEST_MODE=native
